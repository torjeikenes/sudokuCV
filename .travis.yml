os: linux
dist: focal
language: python
python:
  - "3.8"
before_install:
  - sudo apt-get install tesseract-ocr
cache: pip
install:
  - pip install -r requirements.txt
script:
  - python -m unittest discover -v
